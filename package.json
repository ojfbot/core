{
  "name": "ojf-workflow-framework",
  "private": true,
  "version": "0.1.0",
  "description": "Unified slash-command workflow framework for CLI and VS Code",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit -p tsconfig.base.json || true"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
