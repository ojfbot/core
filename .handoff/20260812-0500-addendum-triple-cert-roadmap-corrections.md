# Addendum — corrections for `cca-prep/claude/07-triple-cert-roadmap.md` gate revision

Date: 2026-08-12 · From: core campaign session · For: whichever session bakes the gate-based
track into 07-triple-cert-roadmap.md (the cowork session offered; operator directed the core
session to fold it into the knowledge graph first).

Canonical incorporated version: `~/selfco/wiki/synthesis/certification-ladder-fde.md` — the
gate structure is adopted (gates not dates, Gate-0 acceleration to ~Aug 21–22 conditional on
measurement gates, Gate-1 pre-decided fork, 14-day retake buffer, end-of-Oct honest
commitment). When revising 07, carry these three corrections — do not transcribe the cowork
text as-is:

1. **Gate 2 rescope (the material one).** The claim "the RAG layer and the eval harness are
   capabilities your fleet has been missing by your own audits" is REFUTED by the fleet:
   buddy-check is a full SME-calibrated judge harness; dive-briefing is hybrid retrieval with
   per-claim citation verification; the newline-curriculum ingestion already recorded evals
   (U5) and retrieval (U9) as covered. Gate 2 = translation + case study (buddy-check's
   judge-vs-SME calibration written as the G1 "how do you know it works" answer; dive-briefing
   retrieval design at exam vocabulary), ~1.5 weeks — not 3 weeks of re-building toys.
2. **Add the artifact track.** Certs are a screening credential, not an FDE deliverable
   genre. Every gate emits a G-mapped public artifact (blogengine); the flagship is the
   cca-prep validity story (pre-registered domain-profile prediction scored against the real
   score report after Gate 0).
3. **Re-aim the absorber warning.** Switchboard-risk is real, but the bigger absorption risk
   is the ladder eating **G3 (Texas R&R)** — the FDE audit's confirmed #1 gap. Constraint:
   cert work rides evenings + the drill floor; Texas R&R keeps weekend build slots.

Demote the 4-week calendar to "sprint variant," per the cowork suggestion — that part stands.
