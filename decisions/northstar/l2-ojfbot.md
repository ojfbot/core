---
type: northstar
slug: l2-ojfbot
tier: L2
ladders_up_to: l3-shared
status: active
properties:
  - id: P1
    name: "The fleet ships demoable Frame OS surfaces"
    target: "The shell host plus ≥3 MF remotes load, interoperate, and record the cross-domain hero demo. Each active app is past scaffold."
    current: 55
    verification: "Recorded demo: shell + ≥3 remotes in one flow; shell registration manifest lists them."
    ladders_up_to: "ns:l3-shared#P1"
  - id: P2
    name: "Every app's daily work traces to a measurable property"
    target: "100% of active fleet apps have a northstar; the daily standup frames each priority against a property; movement is recorded as a time-series, not asserted from memory."
    current: 26
    verification: "northstar-lint: every tracked repo has a northstar that ladders here; standup output cites ns:<slug>#P refs; status.jsonl accrues movement lines."
    ladders_up_to: "ns:l3-shared#P2"
---

# Northstar — ojfbot (L2)

**Vision.** ojfbot is the dev platform and Frame OS fleet that turns daily agentic work into shipped
product. It advances the shared apex by being the venture that *ships* (P1) and by making its own
fleet-wide work legible (P2) — the local instance of the cluster-wide "work is self-measuring" bet.

## P1 — The fleet ships demoable Frame OS surfaces

Ladders to `ns:l3-shared#P1` (a working product cluster). Rolls up from the L1 per-app northstars:
each app's delivery property feeds this. Most active fleet apps ladder here; selfco-venture surfaces
ladder to `l2-selfco` instead.

## P2 — Every app's daily work traces to a measurable property

Ladders to `ns:l3-shared#P2` (work is legible). This is the property the three-tier-northstar +
session-provenance work advances directly: standup framing, per-app northstars, recorded movement.
