# Synthesis Ledger (captured from Notion)

> Git backup of the Northstar Roadtrip relay's **Synthesis Ledger** page. Captured 2026-06-29.
> The compounding cross-project thread — Code folds each leg's `SYNTHESIS:` line in here after landing.
> Source: https://app.notion.com/p/38d54a8c53d78163ae03c10a93ffb872

**State at capture: EMPTY (seed only).** No legs have landed, so nothing has been folded in yet — this
confirms 0 completed brief→confirm→land loops. The per-leg `SYNTHESIS:` content authored so far lives
inside the confirmed blocks (`confirmed/shell.md`, `confirmed/blogengine.md`), not yet aggregated here.

---

_No legs landed yet. First leg: f1-substrate (F1 stack)._

## 2026-07-30 — GOLF UMBRELLA landing (sitting 2026-07-27)

- **capture-agent** (`l1-capture-agent`, landed; renamed from gcgcca): SYNTHESIS: cluster-golf's
  first principle — the TX golf-course corpus → segmentation model → Hugging Face portfolio
  artifact feeds fairway (3D twin), golf-research (prediction-vs-ground-truth loop), golf-press
  (methodology articles), and golf-runner ("proprietary course understanding" proof point).
  Intended cluster ref: `cluster-golf#P1` (tier designed-not-built; recorded here only). Prior
  vault research binds: NAIP-CHM fastest path to canopy; NDVI poor tree-vs-turf on golf courses.
  ⚠ Overlap with mirrorworld P6/PH5 (course-feature classification) is an open operator question.
- **fairway** (`l1-fairway`, landed; decomposed out of mirrorworld): SYNTHESIS: first
  consumer/producer decomposition of the roadtrip (foundry-recipes → asset-foundry shape) and the
  cluster's first `depends_on` edge (`ns:l1-capture-agent#P1`). The hero demo serves golf-runner's
  50/50 wedge pitch; mirrorworld remains producer (terrain/bundles), NOT a cluster member.
  Intended cluster ref: `cluster-golf#P2`. Membership ruling: cluster members are REPOS,
  uniformly — the property-subset branch is dead (dissolves itinerary 11b sharp edge 4 for golf).
