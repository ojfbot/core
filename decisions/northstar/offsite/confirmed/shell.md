# Captured confirmed northstar — shell (Frame)

> **Raw relay capture — NOT a landed northstar.** Pulled verbatim from the Notion Itinerary row
> "Frame (shell)" (Status: `confirmed`) on 2026-06-29, to back up authored content that otherwise lived
> only in Notion. This is **not** registered, **not** at `shell/.claude/northstar.md`, and **not**
> enforced by `northstar-lint.mjs`. Landing it is intentionally **HELD** — its block graduates schema
> changes (cluster tier `ns:cluster-<name>@<semver>`, semver-pinned refs, "instance" as the federation
> noun) that are **not yet folded into `schema.md`/lint** (see `../schema-evolution-log.md`).
> Source: https://app.notion.com/p/38d54a8c53d78130b1cbef8df26c9328

```
CONFIRMED NORTHSTAR
app: shell
schema: v1.1
vision: Frame (the shell repo) is a session surface that describes, spawns, manages, and navigates running instances of agentic applications. "Instance" is used in the object-oriented sense: core's launcher registrations are the class (the spec of how to spawn an app), a running app is an instance of it, and a cluster is a collection of instances with a shared lifecycle. The shell does not own the spawn primitive — that lives in core's launcher and is orchestrated by workstation-yuri — the shell is the authorizing surface in front of it, providing insight (describe/observe running instances) and authorized mechanism (spawn/manage/teardown/navigate), with every action authenticated and authorization-checked at the surface boundary under zero-trust. The current Carbon module-federation product-dashboard compositor is disposable; the durable compass is instance-federation, not module-federation. Standalone apps (e.g. f1-pit-wall, ADR-0001) that refused to be UI modules are first-class instances here — they are federated as running instances while keeping their own stacks.
P1 | name: Session surface — describe and navigate running instances | target: A shell session enumerates, describes, and navigates to running agentic-app instances from one navigable surface, replacing the static module compositor with a live instance view | current: 12 | verification: shell holds a live instance registry; can list running instances with health/state; navigate-to resolves to a running instance; module-compositor code does not count toward this | ladders_up_to: ns:l2-ojfbot@0.1.0#P1 | depends_on: | LADDER_STRESS: l2#P1=strain — current P1 wording ("Frame-composed surfaces") strains against standalone-instance surfaces; this is the 2nd instance pointing at the L2-P1-widening gap (substrate leg 1 was the 1st); gate trips at 3
P2 | name: Authorized mechanism — mediate instance lifecycle | target: The shell triggers spawn / manage / teardown lifecycle actions on instances, routing to core's launcher; the shell mediates and authorizes, the launcher executes | current: 8 | verification: shell lifecycle endpoints route to core launcher and return instance state transitions; spawn/teardown observable through the surface; routing-coupling to core launcher is a SYNTHESIS reference, not a depends_on cap (shell does not inherit launcher readiness) | ladders_up_to: ns:l2-ojfbot@0.1.0#P1 | depends_on: | LADDER_STRESS: l2#P1=strain — same standalone/instance-surface gap as P1
P3 | name: Zero-trust boundary on instance actions | target: No unauthenticated call can spawn, navigate-to, or manage any instance; every action carries identity and is authorization-checked at the surface boundary; no ambient trust between shell and the instances it manages; actions are auditable | current: 5 | verification: every lifecycle/navigation endpoint rejects unauthenticated and unauthorized calls; audit log records who-acted-on-what; no ambient-trust path exists | ladders_up_to: ns:l2-ojfbot@0.1.0#P2 | depends_on: | LADDER_STRESS: l2#P2=clean — an auditable authorization boundary is fleet self-measurement
P4 | name: Runtime cluster management | target: The shell spawns, navigates, and manages clusters of instances as a unit (e.g. the F1 stack spawns/tears down together), not only individual instances; a cluster is a collection of instances with shared lifecycle | current: 0 | verification: shell can spawn/teardown a named cluster as one operation; navigate within a cluster; cluster lifecycle state is observable | ladders_up_to: ns:l2-ojfbot@0.1.0#P1 | depends_on: | LADDER_STRESS: l2#P1=strain — same standalone/instance-surface gap
SYNTHESIS: The spawn primitive is a recompose, not a greenfield build — it exists scattered across core/scripts/launcher/registrations + launch.sh (spawn) and workstation-yuri (orchestration); the shell surfaces and authorizes that capability rather than reimplementing it. Routing edge: shell P2 routes to core's launcher (forward reference ns:l1-core#P-launcher, unresolved until core's leg lands — flag, do not cap). Cross-app: f1-pit-wall is a candidate shell-managed instance (instance-federation is compatible with its ADR-0001 standalone posture); leg 1 (f1) and leg 5 (Frame) are no longer independent — when f1's cluster northstar is authored, pit-wall may carry a navigable-from-shell edge. Zero-trust pattern recurs: shell P3 (authorize instance actions) and blog-engine's Phase-C security blocker (uncredentialed JWT issuance, missing userId filtering, cross-user exposure) are the same underlying axis — the fleet lacks a consistent auth/authorization boundary between surfaces; candidate future L2/fleet concern. FRAMEWORK FLAGS THIS LEG: (1) cluster tier graduates to designed — ref syntax ns:cluster-<name>@<semver>#P<n>, an optional rung between L1-app and L2-ojfbot; apps may ladder to a cluster property OR directly to L2. (2) semver-on-refs graduates to designed — all ladder/depends_on refs carry @<semver> pins (pre-1.0 = 0.x, free to break); a child pinned to @0.1.0 against a target bumped to a higher MAJOR is flagged for re-validation (this is the safe mechanism that un-defers the @v2 silent-re-pointing concern — revision becomes non-silent). (3) L2-P1-widening now at 2 instances (substrate, Frame); widening "ships demoable Frame OS surfaces" to "Frame-composed OR standalone instances" would be a MAJOR bump of ns:l2-ojfbot; logged-and-held, gate trips at 3. (4) naming: federation unit bound as "instance" (OOP sense); "remote" (MF noun) is retired.
```
