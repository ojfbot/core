# Captured confirmed northstar — blogengine

> **Raw relay capture — NOT a landed northstar.** Pulled verbatim from the Notion Itinerary row
> "blogengine" (Status: `confirmed`) on 2026-06-29. **Not** registered, **not** at
> `blogengine/.claude/northstar.md`, **not** lint-enforced. Landing **HELD** alongside the Frame pair
> (it carries semver-pinned refs `@0.1.0` not yet in `schema.md`/lint; see `../schema-evolution-log.md`).
> Source: https://app.notion.com/p/38d54a8c53d78164a280cf43854fd322

```
CONFIRMED NORTHSTAR
app: blogengine
schema: v1.1
vision: Blog-engine is the real agent factory for publishing pipelines: config-driven pipelines that take generated content and emit it across multiple formats and platforms (Hugo, WordPress, Medium, Notion), with the declared-but-stubbed capabilities filled in. Today it is a factory that makes exactly one thing — markdown blog drafts from a single hard-wired 9-node LangGraph graph — with publisher, notion-integration, and rag-service as empty stubs and a dead content-type taxonomy the generator ignores. The compass is to decompose that monolith back into a true factory of pipelines, and to make its signature differentiator (the collaborative "yes, and..." Media/Podcast Responder tone) honestly measured rather than silently force-passed. It is the shared publishing layer the fleet's teaching harnesses route into — a plug-in, not part of any single domain stack.
P1 | name: The factory diversifies output | target: Config-driven pipelines emit content to multiple platforms (Hugo/WordPress/Medium/Notion) in multiple formats; the dead ContentTypeSchema taxonomy drives content-type-conditioned generator nodes via the nextAction router; the export {} stubs (publisher, notion-integration, rag-service) are real implementations | current: 15 | verification: each declared publisher target has a working implementation with integration tests (not just a Zod schema); generator branches on content-type rather than one generic prompt; a single config change adds/removes a pipeline target without code edits | ladders_up_to: ns:l2-ojfbot@0.1.0#P1 | depends_on: | LADDER_STRESS: l2#P1=clean — multi-format/multi-platform publishing is exactly delivery
P2 | name: The tone guarantee is measured and honest | target: The collaborative "yes, and..." tone is verified with a real, measured pass-rate; the silent force-pass after TONE_RETRY_CAP=2 (which sets passed:true with violations intact) is removed or surfaced; an eval measures how often the cap fires | current: 8 | verification: ToneCheckerNode never reports passed:true while violations remain; an eval reports the true tone-pass rate and the force-pass frequency over a corpus; the differentiator's % is backed by a measured number, not an unmeasured escape hatch | ladders_up_to: ns:l2-ojfbot@0.1.0#P2 | depends_on: | LADDER_STRESS: l2#P2=clean — the verification is currently compromised; making it honest IS self-measurement. NOTE: current is near-zero not because tone is bad but because the force-pass means the present pass-rate is unprovable
SYNTHESIS: Blog-engine is the shared publishing factory the teaching harnesses route into (substrate -> teaching interface -> blog-engine), but the coupling is loose: it is a plug-in for anything producing content, not bound to any one stack. Teaching-artifact ingestion from f1-pit-wall does NOT exist today — there is no inbound structured-content endpoint, MediaIngestionNode is a passthrough stub, and the only planned inbound pipeline is daily-logger -> blogengine (Phase 9, unbuilt). So the pit-wall -> blog-engine teaching edge is a 0% aspiration named here, NOT a depends_on (no endpoint exists on either side to make it contingent yet); it becomes a real edge when an inbound endpoint is specced. Shared stack with cv-builder/TripPlanner (pnpm monorepo, JWT+thread-ownership, Carbon, LangGraph signature, SSE). Security: /api/tools is off-contract (diverges from ADR-0007) and Phase-C-blocked (TD-009/011/017: uncredentialed JWT issuance, no userId filtering, cross-user exposure) — same zero-trust axis as Frame shell's P3; the fleet lacks a consistent auth boundary between surfaces. Candidate property deferred: the advertised-vs-built capability gap (what /api/tools claims vs what exists) may itself be worth measuring as an honesty property — logged, not authored, pending James's call. All refs pinned @0.1.0 (pre-1.0, free to break).
```
