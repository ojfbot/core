node_modules/
dist/
*.js.map
*.d.ts.map
.env

# Workflow run outputs — created at runtime by writeRun() in
# packages/workflows/src/utils/runs.ts (called from runner.ts). Never committed.
runs/

# Claude Code local session settings (machine-specific permission allows)
.claude/settings.local.json
.claude/settings.json

# Personal knowledge — career context moved to ~/selfco/career/ by decomposition S2
# (2026-08-08). The directory now holds only a local pointer README. Kept ignored so
# career material can never be re-tracked here by accident.
personal-knowledge/

# Claude Code worktree storage (git worktrees created under .claude/)
.claude/worktrees/

# Serena MCP server project state (cache + project config)
.serena/

# Python bytecode — vault skill scripts gained their first importable module
# (schema_check.py) in 2026-07, so __pycache__ now appears wherever lint.py runs.
__pycache__/
*.pyc
