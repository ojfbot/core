Perform a reconnaissance of the codebase or the specified path. Produce a structured report covering:

1. **Directory structure** — high-level layout, key directories and what they contain.
2. **Entry points** — main binaries, exported packages, CLI commands, API surfaces.
3. **Technology stack** — languages, frameworks, key dependencies and their roles.
4. **Architecture patterns** — how the code is organized (monorepo, layered, event-driven, etc.).
5. **Data flows** — how data moves through the system (if discernible from static analysis).
6. **Notable observations** — anything unusual, concerning, or worth calling out (tech debt, security surface, missing tests, etc.).

Keep the report dense and technical — this is for an engineer who needs to get productive quickly.

$ARGUMENTS
