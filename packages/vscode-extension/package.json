{
  "name": "core-vscode-extension",
  "displayName": "Core Workflow",
  "version": "0.1.0",
  "description": "VS Code extension for running core slash-command workflows",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": ["Other"],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "core.runSlashCommand",
        "title": "Core: Run Slash Command"
      },
      {
        "command": "core.listCommands",
        "title": "Core: List Available Commands"
      }
    ]
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "vscode:prepublish": "pnpm run build"
  },
  "dependencies": {
    "@core/workflows": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.5.0"
  }
}
