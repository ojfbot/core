{
  "name": "@core/cli",
  "version": "0.1.0",
  "description": "CLI runner for the core slash-command workflow framework",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "core-workflow": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@core/workflows": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
