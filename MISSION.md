# MISSION — task 2.5: built-in tool selection & sequencing

**Capability sought:** Given an exam scenario describing a codebase exploration or modification
job, produce the canon-ordered built-in tool sequence (which tool moves first, what follows,
and why the tempting alternative is wrong) — reliably enough to convert task 2.5 from 0/3 to
passing on fresh items.

**What they can already do:** Tool *identity* is solid — the operator knows what Grep, Glob,
Read, Edit, and Bash each do (record 0002: "tool identity is fine; the misses are on ordering
and enumeration"). Daily Claude Code fluency is high; the gap is exam-canon *sequencing*
doctrine, not usage.

**Where the current model is wrong (from picked distractors):**
- H44: picked grep-the-file-path for wrapper tracing; canon is enumerate-exports-first,
  then search each name.
- H45: picked Glob-plus-batch-read for codebase understanding; canon is Grep entry points,
  then Read to follow imports incrementally.
- Echoed in N21 (original deck). Pattern: under pressure, reaches for the bulk/direct move
  where canon wants the incremental/enumerated one.

**Forcing function:** CCAR-F sit ~2026-09-08. Task 2.5 is the single worst task and sits in
d2, the weakest weighted domain (55% hard first-attempt). Batch 3 (H73–H75, unseen) is the
treated re-measurement — this lesson is the treatment, so it must not quote or reconstruct
those items.

**Tonight's scope:** one lesson + retrieval quiz, then review the two *already-seen* misses
(H44, H45) in the drill app. Nothing from unseen decks.
